explain call(),apply(),blind() methods.give an examples of call(),apply(),blind().
call(): call method invokes a function.these are just different invoking  or calling a function. 
when you are calling a function using any of these 3 then you don;t have to define in the function that it will be taking as a parameter.
and it will assume that the 1st argument. 
in case of call each and every argument which is required to passed to the function will be individually.
example: 